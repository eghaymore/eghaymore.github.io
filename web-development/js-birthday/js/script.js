var name = prompt("Enter your name");
var age = prompt("Enter your age");
if (age >= 50)
{
	alert("Hello " + name + "!" + " You are " + age + " years old! You're an old-timer.");
}
else
{
	alert("Hello " + name + "!" + " You are " + age + " years old! You'll be old soon!");
}